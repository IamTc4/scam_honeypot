# Procfile for Heroku/Railway deployment

# Voice Detection Service
web: uvicorn voice_detection.app:app --host 0.0.0.0 --port $PORT

# Alternative for Scam Honeypot (if deploying separately)
# web: uvicorn scam_honeypot.app:app --host 0.0.0.0 --port $PORT
